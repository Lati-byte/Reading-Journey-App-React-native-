import * as React from 'react';
import { View, Text } from 'react-native';

export default function profileScreen({navigation}) {
    return(
        <View>
            <Text>We are still baking at profileScreen</Text>
        </View>
    );
}